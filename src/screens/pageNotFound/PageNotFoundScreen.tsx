import React, { FC } from 'react'

type Props = {}

export const PageNotFoundScreen: FC<Props> = (props) => {
    return (
        <div className="PageNotFoundScreen">
            Page not found
        </div>
    )
}